# 🛠️ Guia Técnico de Implementação - AgroVision\n\n**Data: 16 de Junho de 2026**\n**Versão: 1.0**\n\n---\n\n## 📋 Índice\n\n1. [Padrões de Codificação](#padrões-de-codificação)\n2. [Schema PostgreSQL](#schema-postgresql)\n3. [APIs (Go)](#apis-go)\n4. [Serviços IA (Python)](#serviços-ia-python)\n5. [Flutter Widgets](#flutter-widgets)\n6. [Testes TDD](#testes-tdd)\n7. [Integração Docker](#integração-docker)\n\n---\n\n## 📐 Padrões de Codificação\n\n### Go Backend\n\n**Estrutura de Serviço:**\n\n```go\n// service.go\ntype PropertyService struct {\n    db   *sql.DB\n    repo PropertyRepository\n    log  Logger\n}\n\nfunc (s *PropertyService) CreateProperty(ctx context.Context, req CreatePropertyRequest) (*Property, error) {\n    // Validação\n    if err := req.Validate(); err != nil {\n        return nil, fmt.Errorf(\"invalid request: %w\", err)\n    }\n    \n    // Negócio\n    prop := Property{\n        ID:        uuid.New().String(),\n        Name:      req.Name,\n        Area:      req.Area,\n        CreatedAt: time.Now(),\n    }\n    \n    // Persistência\n    if err := s.repo.Save(ctx, &prop); err != nil {\n        return nil, fmt.Errorf(\"failed to save: %w\", err)\n    }\n    \n    return &prop, nil\n}\n```\n\n**Handler HTTP:**\n\n```go\n// handler.go\nfunc (h *Handler) CreateProperty(w http.ResponseWriter, r *http.Request) {\n    ctx := r.Context()\n    \n    var req CreatePropertyRequest\n    if err := json.NewDecoder(r.Body).Decode(&req); err != nil {\n        http.Error(w, err.Error(), http.StatusBadRequest)\n        return\n    }\n    \n    prop, err := h.svc.CreateProperty(ctx, req)\n    if err != nil {\n        http.Error(w, err.Error(), http.StatusInternalServerError)\n        return\n    }\n    \n    w.Header().Set(\"Content-Type\", \"application/json\")\n    json.NewEncoder(w).Encode(prop)\n}\n```\n\n### Python AI Service\n\n**Estrutura:**\n\n```python\n# services/ai_service.py\nfrom fastapi import FastAPI\nfrom pydantic import BaseModel\nimport logging\n\napp = FastAPI()\nlogger = logging.getLogger(__name__)\n\nclass AIResponse(BaseModel):\n    question: str\n    answer: str\n    confidence: float\n    data_sources: list[str]\n\n@app.post(\"/ask\")\nasync def ask_assistant(question: str, user_id: str) -> AIResponse:\n    \"\"\"\n    Assistente rural inteligente\n    \"\"\"\n    try:\n        # 1. Recuperar dados da propriedade\n        property_data = await fetch_property_data(user_id)\n        \n        # 2. NLP para entender intenção\n        intent = extract_intent(question)\n        \n        # 3. Recuperar dados contextuais\n        context = build_context(property_data, intent)\n        \n        # 4. Gerar resposta\n        answer, confidence, sources = generate_answer(question, context)\n        \n        return AIResponse(\n            question=question,\n            answer=answer,\n            confidence=confidence,\n            data_sources=sources\n        )\n    except Exception as e:\n        logger.error(f\"Error in ask_assistant: {e}\")\n        raise\n```\n\n### Flutter\n\n**Padrão MVVM:**\n\n```dart\n// models/property.dart\nclass Property {\n  final String id;\n  final String name;\n  final double area;\n  final DateTime createdAt;\n\n  Property({\n    required this.id,\n    required this.name,\n    required this.area,\n    required this.createdAt,\n  });\n\n  factory Property.fromJson(Map<String, dynamic> json) {\n    return Property(\n      id: json['id'],\n      name: json['name'],\n      area: (json['area'] as num).toDouble(),\n      createdAt: DateTime.parse(json['createdAt']),\n    );\n  }\n}\n\n// services/property_service.dart\nclass PropertyService {\n  final ApiClient client;\n\n  PropertyService(this.client);\n\n  Future<Property> createProperty({\n    required String name,\n    required double area,\n  }) async {\n    final response = await client.post(\n      '/properties',\n      data: {'name': name, 'area': area},\n    );\n    return Property.fromJson(response.data);\n  }\n}\n\n// screens/property/property_viewmodel.dart\nclass PropertyViewModel extends ChangeNotifier {\n  final PropertyService service;\n  final List<Property> properties = [];\n  bool isLoading = false;\n  String? error;\n\n  PropertyViewModel(this.service);\n\n  Future<void> createProperty(String name, double area) async {\n    isLoading = true;\n    notifyListeners();\n    \n    try {\n      final prop = await service.createProperty(name: name, area: area);\n      properties.add(prop);\n      error = null;\n    } catch (e) {\n      error = e.toString();\n    } finally {\n      isLoading = false;\n      notifyListeners();\n    }\n  }\n}\n\n// screens/property/property_screen.dart\nclass PropertyScreen extends StatelessWidget {\n  @override\n  Widget build(BuildContext context) {\n    return ChangeNotifierProvider(\n      create: (_) => PropertyViewModel(PropertyService(ApiClient())),\n      child: PropertyScreenContent(),\n    );\n  }\n}\n\nclass PropertyScreenContent extends StatelessWidget {\n  @override\n  Widget build(BuildContext context) {\n    final vm = context.watch<PropertyViewModel>();\n    \n    return Scaffold(\n      appBar: AppBar(title: Text('Propriedades')),\n      body: vm.isLoading\n          ? Center(child: CircularProgressIndicator())\n          : ListView.builder(\n              itemCount: vm.properties.length,\n              itemBuilder: (context, i) => PropertyTile(vm.properties[i]),\n            ),\n    );\n  }\n}\n```\n\n---\n\n## 🗄️ Schema PostgreSQL\n\n### Tabelas Principais\n\n```sql\n-- Users & Autenticação\nCREATE TABLE users (\n    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    email VARCHAR(255) UNIQUE NOT NULL,\n    password_hash VARCHAR(255) NOT NULL,\n    full_name VARCHAR(255),\n    role VARCHAR(50) DEFAULT 'farmer',\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n\n-- Propriedades\nCREATE TABLE properties (\n    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,\n    name VARCHAR(255) NOT NULL,\n    total_area DECIMAL(10, 2),\n    planted_area DECIMAL(10, 2),\n    location_lat DECIMAL(10, 8),\n    location_lng DECIMAL(11, 8),\n    soil_type VARCHAR(100),\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n\n-- Talhões\nCREATE TABLE plots (\n    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    property_id UUID NOT NULL REFERENCES properties(id) ON DELETE CASCADE,\n    name VARCHAR(255) NOT NULL,\n    area DECIMAL(10, 2),\n    crop_type VARCHAR(50), -- 'coffee', 'pepper', 'cocoa', etc\n    crop_variety VARCHAR(100),\n    planting_date DATE,\n    number_of_plants INTEGER,\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n\n-- Produção\nCREATE TABLE production (\n    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    plot_id UUID NOT NULL REFERENCES plots(id) ON DELETE CASCADE,\n    harvest_date DATE NOT NULL,\n    quantity_kg DECIMAL(10, 2),\n    quality_grade VARCHAR(50),\n    notes TEXT,\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n\n-- Custos\nCREATE TABLE expenses (\n    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    property_id UUID NOT NULL REFERENCES properties(id) ON DELETE CASCADE,\n    category VARCHAR(50), -- 'fertilizer', 'fuel', 'labor', etc\n    amount DECIMAL(12, 2) NOT NULL,\n    date DATE NOT NULL,\n    description TEXT,\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n\n-- Estoque\nCREATE TABLE stock (\n    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    property_id UUID NOT NULL REFERENCES properties(id) ON DELETE CASCADE,\n    item_name VARCHAR(255) NOT NULL,\n    category VARCHAR(50), -- 'fertilizer', 'pesticide', etc\n    quantity DECIMAL(10, 2),\n    unit VARCHAR(20), -- 'kg', 'liters', etc\n    minimum_quantity DECIMAL(10, 2),\n    expiry_date DATE,\n    last_updated TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n\n-- Alertas Climáticos\nCREATE TABLE weather_alerts (\n    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    property_id UUID NOT NULL REFERENCES properties(id) ON DELETE CASCADE,\n    alert_type VARCHAR(50), -- 'rain', 'frost', 'drought', etc\n    severity VARCHAR(20), -- 'low', 'medium', 'high'\n    description TEXT,\n    recommended_action TEXT,\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n    resolved_at TIMESTAMP\n);\n\n-- Máquinas\nCREATE TABLE machines (\n    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n    property_id UUID NOT NULL REFERENCES properties(id) ON DELETE CASCADE,\n    name VARCHAR(255),\n    type VARCHAR(100), -- 'trator', 'colheitadeira', etc\n    purchase_date DATE,\n    horimeter INTEGER DEFAULT 0,\n    fuel_consumption DECIMAL(5, 2),\n    last_maintenance_date DATE,\n    next_maintenance_date DATE,\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n```\n\n### Indices para Performance\n\n```sql\nCREATE INDEX idx_properties_user_id ON properties(user_id);\nCREATE INDEX idx_plots_property_id ON plots(property_id);\nCREATE INDEX idx_production_plot_id ON production(plot_id);\nCREATE INDEX idx_expenses_property_id ON expenses(property_id);\nCREATE INDEX idx_expenses_date ON expenses(date);\nCREATE INDEX idx_weather_alerts_property_id ON weather_alerts(property_id);\n```\n\n---\n\n## 🔌 APIs (Go)\n\n### Property Service Endpoints\n\n```\nPOST   /api/v1/properties              Create property\nGET    /api/v1/properties              List user properties\nGET    /api/v1/properties/{id}         Get property details\nPUT    /api/v1/properties/{id}         Update property\nDELETE /api/v1/properties/{id}         Delete property\n\nPOST   /api/v1/properties/{id}/plots   Create plot\nGET    /api/v1/properties/{id}/plots   List plots\nDELETE /api/v1/plots/{id}              Delete plot\n```\n\n### Production Service Endpoints\n\n```\nPOST   /api/v1/plots/{id}/production          Register harvest\nGET    /api/v1/plots/{id}/production          Get production history\nGET    /api/v1/plots/{id}/production/stats    Get productivity stats\nGET    /api/v1/properties/{id}/production     Aggregate by property\n```\n\n### Financial Service Endpoints\n\n```\nPOST   /api/v1/properties/{id}/expenses       Record expense\nGET    /api/v1/properties/{id}/expenses       List expenses\nGET    /api/v1/properties/{id}/financial      Get financial summary\nGET    /api/v1/properties/{id}/metrics        Get cost metrics\n```\n\n---\n\n## 🤖 Serviços IA (Python)\n\n### Endpoints\n\n```\nPOST   /api/v1/ai/ask                 Chat com assistente\nPOST   /api/v1/vision/analyze         Analisar imagem\nPOST   /api/v1/voice/transcribe       Converter voz em texto\nGET    /api/v1/predictions/{id}       Previsão de safra\n```\n\n### Modelos de ML (Exemplos)\n\n```python\n# Previsão de Produção\nfrom sklearn.ensemble import RandomForestRegressor\nimport numpy as np\n\nclass ProductionPredictor:\n    def __init__(self):\n        self.model = RandomForestRegressor(n_estimators=100)\n    \n    def train(self, X, y):\n        \"\"\"Treina com histórico de propriedade\"\"\"\n        self.model.fit(X, y)\n    \n    def predict_harvest(self, features):\n        \"\"\"Prediz produção em kg/hectare\"\"\"\n        return self.model.predict([features])[0]\n\n# Diagnóstico de Doenças (YOLO)\nimport cv2\nfrom ultralytics import YOLO\n\nclass DiseaseDetector:\n    def __init__(self):\n        self.model = YOLO('yolov8n.pt')  # ou modelo customizado\n    \n    def detect(self, image_path, crop_type):\n        \"\"\"Detecta pragas/doenças em foto\"\"\"\n        img = cv2.imread(image_path)\n        results = self.model(img)\n        return self._parse_results(results, crop_type)\n```\n\n---\n\n## 🎨 Flutter Widgets\n\n### Dashboard Principal\n\n```dart\nclass DashboardScreen extends StatelessWidget {\n  @override\n  Widget build(BuildContext context) {\n    return SingleChildScrollView(\n      child: Column(\n        children: [\n          // KPIs\n          ProductionCard(),\n          FinancialCard(),\n          HealthCard(),\n          \n          // Alertas\n          AlertsList(),\n          \n          // Gráficos\n          ProductionChart(),\n          CostChart(),\n        ],\n      ),\n    );\n  }\n}\n```\n\n---\n\n## ✅ Testes TDD\n\n### Go\n\n```go\n// services/property_service_test.go\nfunc TestCreateProperty(t *testing.T) {\n    repo := NewMockRepository()\n    svc := NewPropertyService(repo)\n    \n    prop, err := svc.CreateProperty(context.Background(), CreatePropertyRequest{\n        Name: \"Fazenda X\",\n        Area: 100.0,\n    })\n    \n    assert.NoError(t, err)\n    assert.NotNil(t, prop)\n    assert.Equal(t, \"Fazenda X\", prop.Name)\n}\n```\n\n### Python\n\n```python\n# tests/test_ai_service.py\nfrom fastapi.testclient import TestClient\nfrom services.ai_service import app\n\nclient = TestClient(app)\n\ndef test_ask_assistant():\n    response = client.post(\n        \"/ask\",\n        json={\n            \"question\": \"Qual meu custo por saca?\",\n            \"user_id\": \"user123\"\n        }\n    )\n    \n    assert response.status_code == 200\n    assert \"answer\" in response.json()\n```\n\n### Flutter\n\n```dart\n// test/services/property_service_test.dart\nvoid main() {\n  test('PropertyService creates property', () async {\n    final mockClient = MockApiClient();\n    final service = PropertyService(mockClient);\n    \n    final prop = await service.createProperty(\n      name: 'Fazenda X',\n      area: 100.0,\n    );\n    \n    expect(prop.name, 'Fazenda X');\n    expect(prop.area, 100.0);\n  });\n}\n```\n\n---\n\n## 🐳 Integração Docker\n\n### docker-compose.yml\n\n```yaml\nversion: '3.9'\nservices:\n  postgres:\n    image: postgres:15\n    environment:\n      POSTGRES_PASSWORD: agrovision\n      POSTGRES_DB: agrovision_db\n    ports:\n      - \"5432:5432\"\n    volumes:\n      - postgres_data:/var/lib/postgresql/data\n  \n  api-gateway:\n    build:\n      context: ./services/api-gateway\n      dockerfile: Dockerfile\n    ports:\n      - \"8080:8080\"\n    depends_on:\n      - postgres\n    environment:\n      DATABASE_URL: postgresql://postgres:agrovision@postgres:5432/agrovision_db\n  \n  ai-service:\n    build:\n      context: ./services/ai-service\n      dockerfile: Dockerfile\n    ports:\n      - \"8001:8001\"\n    depends_on:\n      - postgres\n    environment:\n      DATABASE_URL: postgresql://postgres:agrovision@postgres:5432/agrovision_db\n\nvolumes:\n  postgres_data:\n```\n\n---\n\n**Versão: 1.0**  \n**Último update: 16 de Junho de 2026**\n